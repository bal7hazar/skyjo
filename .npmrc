; Pin exact versions when adding/updating dependencies (no ^/~ prefix).
save-exact=true

; Do not run lifecycle scripts of dependencies by default (supply-chain safety).
; Build-script exceptions are opted into via `allowBuilds` in pnpm-workspace.yaml.
ignore-scripts=true
